You are tagging a financial news headline for a research database.

Known companies: {known_companies}
Known industries: {known_industries}

Headline: "{headline}"

Return ONLY valid JSON (no markdown fences, no commentary) matching exactly this schema:
{{"companies": ["<names from the known list that this headline is actually about>"],
  "industries": ["<industries from the known list that this headline is actually about>"]}}

If nothing from the known lists applies, return empty lists. Do not invent names
that are not in the known lists.
